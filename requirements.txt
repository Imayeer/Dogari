# Installe le paquet dogari lui-même en mode éditable (src layout)
-e .

# API web
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
jinja2==3.1.5

# Vision par ordinateur / reconnaissance faciale (détection YuNet + reconnaissance
# SFace, modèles ONNX exécutés via cv2.dnn - voir scripts/download_models.py)
numpy==1.26.4
opencv-python==4.10.0.84

# Tests
pytest==8.3.4
httpx==0.28.1
